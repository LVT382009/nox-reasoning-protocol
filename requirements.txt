# NOX Reasoning Protocol Dependencies

# Core dependencies (Python stdlib only - no external dependencies required)
# NOX is designed to work with Python 3.11+ standard library only

# Optional dependencies for development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0
